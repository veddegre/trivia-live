/** Shared password rules for admin / host accounts. */
export const MIN_PASSWORD_LENGTH = 10;
export const MAX_PASSWORD_LENGTH = 200;

export const passwordSchemaMessage = `Password must be at least ${MIN_PASSWORD_LENGTH} characters`;
